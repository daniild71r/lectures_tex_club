\NeedsTeXFormat{LaTeX2e}


%%% Работа с русским языком
\RequirePackage{cmap}					% поиск в PDF
\RequirePackage{mathtext} 				% русские буквы в формулах
\RequirePackage[OT2, LGR, T1, T2A]{fontenc}			% кодировка
\RequirePackage[utf8]{inputenc}			% кодировка исходного текста
\RequirePackage[english,russian]{babel}	% локализация и переносы
% \RequirePackage{indentfirst}            % красная строка в первом абзаце
% \RequirePackage{parskip}
% \RequirePackage{pscyr}
\frenchspacing    

%%% Дополнительная работа с математикой
\RequirePackage{amsmath,amsfonts,amssymb,amsthm,mathtools} % пакеты AMS
\RequirePackage{cancel}
\RequirePackage{nicefrac}
\RequirePackage{wasysym}
\RequirePackage{bbm} % Blackboard bold для цифр
\RequirePackage{icomma}    
\RequirePackage{centernot} % центрированное зачеркивание символа
\RequirePackage{stmaryrd}  % некоторые спецсимволы

%%% Работа с картинками
\RequirePackage{graphicx}    % Для вставки рисунков
\setlength\fboxsep{3pt}  % Отступ рамки \fbox{} от рисунка
\setlength\fboxrule{1pt} % Толщина линий рамки \fbox{}
\RequirePackage{wrapfig}     % Обтекание рисунков текстом
\RequirePackage{capt-of}

%%% Работа с таблицами
\RequirePackage{array,tabularx,tabulary,booktabs} % Дополнительная работа с таблицами
\RequirePackage{longtable}                        % Длинные таблицы
\RequirePackage{multirow}                         % Слияние строк в таблице

%%% Оформление страницы
\RequirePackage{extsizes}     % Возможность сделать 14-й шрифт
\RequirePackage{geometry}     % Простой способ задавать поля
\RequirePackage{setspace}     % Интерлиньяж
\RequirePackage{enumitem}     % Настройка окружений itemize и enumerate
\setlist{leftmargin=25pt} % Отступы в itemize и enumerate

\geometry{top=25mm}    % Поля сверху страницы
\geometry{bottom=30mm} % Поля снизу страницы
\geometry{left=20mm}   % Поля слева страницы
\geometry{right=20mm}  % Поля справа страницы

\setlength\parindent{15pt}        % Устанавливает длину красной строки 15pt
\linespread{1.3}                  % Коэффициент межстрочного интервала
%\setlength{\parskip}{0.5em}      % Вертикальный интервал между абзацами
%\setcounter{secnumdepth}{0}      % Отключение нумерации разделов
%\setcounter{section}{-1}         % Нумерация секций с нуля
\RequirePackage{multicol}			  % Для текста в нескольких колонках
\RequirePackage{soulutf8}   

%%% Колонтитулы
\RequirePackage{titleps}
\newpagestyle{main}{
	\setheadrule{0.4pt}
	\sethead{\CourseName}{}{\hyperlink{intro}{\;Назад к содержанию}}
	\setfootrule{0.4pt}                       
	\setfoot{ФПМИ МФТИ, \CourseDate}{}{\thepage} 
}
\pagestyle{main}  

%%% Гиперссылки
\RequirePackage{hyperref}
\RequirePackage[usenames,dvipsnames,svgnames,table,rgb]{xcolor}
\hypersetup{
	unicode=true,            % русские буквы в раздела PDF
	colorlinks=true,       	 % Цветные ссылки вместо ссылок в рамках
	linkcolor=black!15!blue, % Внутренние ссылки
	citecolor=green,         % Ссылки на библиографию
	filecolor=magenta,       % Ссылки на файлы
	urlcolor=NavyBlue,       % Ссылки на URL
}
\RequirePackage{nicematrix}

%%% Графика
\RequirePackage{tikz}        % Графический пакет tikz
\RequirePackage{tikz-cd}     % Коммутативные диаграммы
\RequirePackage{tkz-euclide} % Геометрия
\RequirePackage{stackengine} % Многострочные тексты в картинках
\RequirePackage{pgfplots}
\usetikzlibrary{calc}
\usetikzlibrary{intersections}
\usetikzlibrary{through}
\usetikzlibrary{quotes}
\usetikzlibrary{angles}
\usetikzlibrary{patterns}
\usetikzlibrary{fpu}
\usetikzlibrary{babel}
\RequirePackage{tikz-3dplot}
\pgfplotsset{compat=1.13}

\RequirePackage{subfiles}